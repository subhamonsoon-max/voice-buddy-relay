name: voice_buddy
description: "Voice Buddy - AI Talking Avatar Companion for Kids"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.13.1
  connectivity_plus: ^6.1.0
  web_socket_channel: ^3.0.1
  flutter_foreground_task: ^11.0.3
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
